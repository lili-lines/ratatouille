title: 🐭 Ratatouille
description: Micromouse with Analog Whiskers
author: lili-lines

# Project site: the site is served under /ratatouille
baseurl: "/ratatouille"
url: "https://lili-lines.github.io"

# Blog theme bundled with GitHub Pages
theme: minima

# Generates blog pages from the _posts folder
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Base URL for source files, browsable on GitHub (not served by the site)
repo: https://github.com/lili-lines/ratatouille/blob/main

# Don't include code/project folders in the site
exclude:
  - algo
  - elect
  - mecha
  - test
